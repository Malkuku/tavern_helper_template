# 手机目录整理

## 状态

已完成；待宿主视觉与 MVU 联调

## 目标与范围

把用户提供的 `types.ts` 保存在手机界面模块，整理手机壳、桌面、控制中心、微信和六个数据应用的文件职责；保留当前界面行为与 MVU 读写路径。

## 审计结论与关键设计决定

- `src/魔法少女恶堕/types.ts` 与用户贴出的接口语义一致，移动到 `src/手机界面/types.ts`，保留单一类型来源。
- 变量 Store 移到手机模块 `store/StatStore.ts`；移除旧目录里的同一份实现。
- `App.vue` 只负责手机浮层、拖拽、时钟与页面切换；桌面和控制中心拆成组件。
- `DataApps.vue` 当前承担六类不同视图、变量选取和公共外框。按应用拆成视图组件，公共外框只处理标题、返回与变量缺失空态。
- 微信搬入 `apps/wechat`；样式随对应组件或应用维护，不再堆在模块根目录。

## Agent 分工

- 当前 Agent 完成整理、静态验证和文档收口。

## 验收标准

- `types.ts` 字段完整，Store 与页面都引用手机模块内的唯一类型文件。
- 手机入口、六个数据应用、微信、桌面与控制中心保持原有显示和交互。
- 旧的聚合组件和跨目录类型/Store 无生产引用，删除失去职责的文件。

## 测试所有权与验证范围

- 当前 Agent：目录与引用检查、定向 ESLint/Prettier、开发构建、数据分支及导航检查。宿主视觉/MVU 联调仍依赖酒馆环境。

## 文档更新项

- 更新架构文档、迭代索引以及旧任务中的稳定路径描述。

## 旧机制取缔项

- 取缔根目录的 `DataApps.vue` 大条件分支与 `src/魔法少女恶堕` 的跨模块类型/Store 路径。
- 取缔 `App.vue` 内的桌面数据清单和控制中心演示状态职责。

## 完成记录

已把 `types.ts`、Store 收入手机模块，移动微信并拆出桌面、控制中心、五个数据视图组件与公共外框；随身物品和仓库共享同一物品结构视图。原 `DataApps.vue` 和旧跨目录类型/Store 已退出生产路径。样式按手机壳、数据应用、微信分区，数据样式限制在 `.data-app` 内。控制中心开关与音量在手机关闭后仍保留。手机模块定向 ESLint、Prettier、Webpack 开发构建通过，TypeScript 全量检查的既有错误仍存在但未报到手机模块。真实宿主视觉与 MVU 联调仍待进行。
