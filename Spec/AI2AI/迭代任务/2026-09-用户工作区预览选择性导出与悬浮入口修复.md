# 2026-09 用户工作区预览、选择性导出与悬浮入口修复

## 状态

自动化验收完成待宿主验收

## 目标与范围

- 用户工作区的待加入角色可先查看结构化档案，再决定加入或替换。
- 资产包导出允许按资产选择；被选剧本所引用的资源自动纳入导出包。
- 创意工坊悬浮入口可在鼠标和触摸设备上实际拖拽，并明确展示“创意工坊”身份。
- 修复移动端主要角色和次要角色编辑界面的类型专属渲染问题。

## 关键设计决定

- 角色预览复用现有角色数据与头像，不创建另一份详情数据模型；加入与替换仍走既有原子写入路径。
- `createPackage` 是导出依赖闭包的权威责任点，界面只提交显式选择，避免调用方遗漏剧本依赖。
- 悬浮入口采用小螈的鼠标/触摸拖拽语义，但监听、视口尺寸和定时器均取自悬浮入口的 `ownerDocument/defaultView`，以固定
  `top/left` 统一宿主坐标系。
- 角色编辑器在当前角色不支持活动分区时回到基本资料；可选集合组件以空集合展示缺失字段，不因浏览旧数据而修改草稿。

## Agent 分工

- 主 Agent：设计、实现、实现自测、Spec 收口。
- 本轮不设独立测试 Agent；依据现有验证脚本补充自动化断言并执行最小构建验证。

## 验收标准

- 角色卡可打开预览，预览显示身份、外貌/简介、背景、性格、技能/术、状态与物品等已有信息，并可从预览执行加入或替换。
- 导出弹窗可按分类与具体资产勾选；空选择不能导出；选择剧本时包内自动包含其全部直接引用资源。
- 悬浮入口有明确文字标识；拖动后不会误触打开，位置被限制在可见视口内，鼠标与触摸共用同一路径。
- 从人格关系分区切换到次要角色后仍显示有效编辑分区；缺少 `语料` 的主要角色可正常显示，窄屏语料操作不产生横向溢出。

## 测试所有权与验证范围

- 在 `scripts/validate-workshop.cts` 覆盖选择性导出和剧本依赖闭包。
- 执行工坊数据验证、lint 与生产构建；真实宿主中的拖拽和 MVU 写入保留为宿主验收项。

## 文档更新项

- 更新资产管理契约中的选择性导出、运行时角色预览和悬浮入口规则。
- 更新索引与任务状态。

## 旧机制取缔项

- 移除用户工作区固定全量导出的唯一入口。
- 移除创意工坊按钮失效的 iframe Pointer Events 坐标路径。
- 移除只支持鼠标且仍依赖错误窗口上下文的 jQuery UI 拖拽路径。

## 完成记录

- 2026-09-18 再次核对宿主反馈后保持 `type + key`
  身份语义不变；修复工坊以具体最后楼层读取 MVU、导致最新有效运行角色漏判的问题。存在性统一读取
  `message_id: -1`，写入仍落到当前最后楼层。
- 2026-09-18 宿主反馈发现运行时角色候选退化为“一版本一卡”，且空 user 被底层误判冲突、冲突返回值被界面忽略；按既有契约恢复“角色身份（type +
  key，user 单例）→版本”结构，并补充原始失败案例回归验收。
- 回归整改后，角色身份/版本互斥、空 user 加入、已有 user 冲突、运行态事务、工坊数据校验、定向 ESLint 和生产构建均通过；待真实宿主确认版本展开、隐藏已有身份与替换确认交互。

- 用户工作区角色卡新增只读档案预览，并允许从预览继续加入或替换。
- 导出入口改为分类/资产选择器，`createPackage` 自动补齐所选剧本的直接依赖。
- 创意工坊悬浮入口改用 Pointer Events 拖拽，增加文字身份、视口约束和位置记忆。
- `validate-workshop`、本次改动定向 lint 与生产构建通过；全仓 lint 仍有 6 个既有错误，不属于本轮改动。
- 待在真实 Tavern Helper / SillyTavern 宿主中验收鼠标与触摸拖拽、角色预览后的运行态写入及文件下载。
- 宿主反馈整改：首版角色预览错误地手写了字段摘要，改为复用 `CharPanel`
  只读模式；移动端全屏改为与尘史使徒一致的动态视口和安全区布局。
- 整改后定向 lint 无错误，生产构建通过；`CharPanel` 的既有属性顺序 lint 警告未在本轮扩散处理。
- 第二次宿主反馈：Pointer Events 版本使用脚本 iframe 视口约束宿主 DOM，坐标系不一致导致按钮仍不可拖拽；改为
  `feature-moelotl` 已验证的宿主 jQuery UI 拖拽路径。
- 小螈同款整改后，创意工坊入口以固定 `top/left` 初始化，jQuery
  UI 在宿主窗口内拖拽，停止时保存脚本变量并延迟恢复点击；定向 lint 与生产构建通过。
- 移动端角色编辑整改：编辑器按资产 ID 重建局部状态，角色类型变化时校正失效分区；主要角色语料缺失时以空集合安全展示，卡片和操作区在窄屏改为可收缩、换行布局。相关文件定向 lint 与生产构建通过。
- 第三次宿主反馈整改：悬浮入口改为在其宿主文档监听原生鼠标和触摸事件，按宿主视口约束位置，并以移动阈值和延迟点击抑制区分拖拽与打开；角色语料的只读行与编辑控件均允许长文本在容器内断行。
- 第三次整改定向 lint 无错误，生产构建通过；共享 `CharPanel` 仍只有既有属性顺序警告，真实宿主触摸拖拽保留为宿主验收项。
