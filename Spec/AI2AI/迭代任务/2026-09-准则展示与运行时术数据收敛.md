# 准则展示与运行时术数据收敛

## 状态

自动化验收完成待宿主验收

## 目标与范围

- 删除开场白固定数据和运行时状态中的顶层 `术` 等级能力表。
- 保留角色 `术之等级`、经验升级、属性派生和技能校验。
- 将角色术详情框改为面向玩家的固定准则介绍，不直接转录面向 AI 的完整设定。

## 关键设计决定

- `世界观与设定/准则` 是事实依据，但玩家文案独立编写，只表达意象、倾向和认知边界。
- 玩家界面不展示逐级固定能力清单、AI 施术推演规则或完整高阶能力边界。
- 准则介绍属于前端展示常量，不进入可变 `stat_data`，也不由开场白重复携带。
- 角色 `术之等级` 继续作为等级、经验和派生数值的唯一运行时来源。

## Agent 分工

- 主 Agent：契约收敛、玩家文案、界面实现、自动化验证与文档收口。
- 实现自测：主 Agent 执行真实 Beta 数据组装、格式检查、静态检查和构建。

## 验收标准

- `开场白.json` 固定数据不再保存 `术`。
- Beta `MvuZod.js` 不再补回或保留顶层 `术`，角色 `术之等级` 仍正常参与计算。
- 剧本组装接受没有顶层 `术` 的数据，输出也不包含该字段。
- 术详情框显示独立玩家介绍及角色当前等级/经验，不再读取逐级能力表。
- 八种准则都有明确且非规则手册式的玩家文本。

## 验证范围

- 真实 Beta 配置的剧本数据验证。
- 创意工坊定向验证、ESLint、Prettier 与开发构建。
- 检索确认生产路径不再读取顶层 `stat_data.术`。

## 文档更新项

- 更新运行时组装契约、技术架构和测试记录中关于固定数据的描述。
- 更新根索引与任务索引状态。

## 旧机制取缔项

- 移除顶层 `术` 静态逐级能力表及其 Zod、TypeScript 契约。
- 移除 `ArtsModule` 从运行时 Store 查找逐级能力描述的路径。

## 完成记录

- 2026-09-21：删除 Beta 开场白固定数据、MvuZod、运行时 Schema 与 TypeScript 类型中的顶层 `术`；角色 `术之等级` 计算链保持不变。
- 2026-09-21：术详情框改为八套面向玩家的固定准则介绍，保留当前等级与经验展示，移除逐级能力表和 Store 查询路径。
- 2026-09-21：创意工坊定向验证、任务范围 ESLint 和全入口开发构建通过；真实 Beta 数据组装通过新增的顶层 `术` 退出断言，随后被既有地图节点数断言阻断（当前数据 121，旧断言 52），与本任务无关。
- 待宿主验收：在真实 SillyTavern 中检查八种准则弹窗的桌面与移动端排版、滚动和文案观感。
