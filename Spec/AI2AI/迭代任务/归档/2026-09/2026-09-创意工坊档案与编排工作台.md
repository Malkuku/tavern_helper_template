# 创意工坊档案与编排工作台

## 状态

自动化验收完成待宿主验收

## 目标与范围

- 将角色编辑从纵向长表单改为档案优先的领域工作台。
- 将剧本编辑改为自由编排工作台，以概览、阵容和叙事模块表达组装结果。
- 新增独立交互式角色选择器，按角色身份分组并互斥选择具体版本。
- 将默认头像从相近的人形轮廓改为可辨识的符号选择。
- 本轮不实现运行时角色阅览弹窗；只建立可复用的档案展示与编辑边界。

## 关键设计决定

- 角色档案默认可阅读；短字段原位编辑，关系、技能、特殊状态和物品通过卡片进入侧边详情。
- 角色选择器使用“角色身份 → 具体版本”二级结构；同 `type + key` 版本互斥，所有 `user` 资产互斥。
- 替换角色版本只更新剧本引用，不删除角色资产；存量覆盖链在草稿中保留原先最终生效版本。
- 剧本以自由编排工作台为权威编辑入口；首次创建向导不在本轮新增，避免形成第二套编辑流程。
- 新组件直接读写现有内存草稿，继续使用现有差异审阅和原子保存。

## Agent 分工

- 主 Agent：设计落地、代码实现、实现自测、验证与文档收口。

## 验收标准

- 角色编辑不再将全部字段纵向铺开；头像和身份摘要常驻，可在领域页签间切换。
- 短字段可以原位编辑；复杂集合以卡片呈现并从侧边详情完成新增、修改和删除。
- 剧本首页可直接判断名称、可玩状态、当前阵容和叙事模块配置情况。
- 点击角色阵容打开独立选择器；可搜索、按类型筛选、查看身份和版本摘要并互斥选择版本。
- 同一身份选择新版本会替换旧引用；选择另一个 `user` 会替换原 `user`；确认前不改变剧本草稿。
- 存量同身份多版本引用进入编辑后保留原先最后生效的版本，不静默保留覆盖链。
- 默认头像提供轮廓明显不同的符号并以图形名称选择，缩小后仍可区分。
- 桌面侧边详情与移动端全屏子页面均可完成编辑，核心操作不依赖 hover。

## 测试所有权与验证范围

- 实现自测：角色互斥选择、存量覆盖链归并、`user` 单例、取消选择不改草稿。
- 静态检查：创意工坊相关 Vue/TypeScript ESLint。
- 构建：全入口开发构建。
- 既有数据回归：创意工坊定向验证与剧本组装验证。
- 宿主人工验收：浮层尺寸、移动端软键盘、侧边详情滚动与真实角色数据密度。

## 文档更新项

- 更新创意工坊字段与界面映射、资产管理、剧本组装、测试方案与任务索引。
- 完成后将讨论草稿中的有效结论迁移并删除草稿。

## 旧机制取缔项

- 取缔 `RoleEditor.vue` 将全部角色字段纵向铺开的长表单职责。
- 取缔 `ScenarioEditor.vue` 以原生角色下拉框追加 UUID 和上下移动决定版本优先级的交互。
- 取缔同一剧本内同角色多版本覆盖链作为正常产品能力。
- 取缔默认头像除样式一外仅改变人形头部轮廓的六样式方案。

## 完成记录

- 2026-09-17：完成角色档案阅读/编辑切换与领域导航；语料、人物关系、技能、术、特殊状态和物品改为卡片及侧边详情编辑。
- 2026-09-17：完成剧本概览、阵容、世界和叙事四区编排工作台，以及按身份与版本组织的交互式角色选择器。
- 2026-09-17：完成角色版本互斥、存量覆盖链归并与组装器重复身份拒绝；默认头像改为旅人、六芒星、匕首、羽毛笔、秘眼和残月。
- 2026-09-17：创意工坊定向验证、相关 ESLint 和全入口开发构建通过；宿主尺寸、移动端详情与真实数据密度待人工验收。
- 2026-09-18：根据宿主验收反馈压缩视觉主题栏，新增秘月、六芒星、蠕虫、林地和白骨门；布尔字段收为紧凑开关，角色阵容卡与选择器详情提高可读宽度并补齐窄屏断行。创意工坊定向验证、相关 ESLint 和全入口开发构建通过，待宿主复核实际密度。
- 2026-09-19：共享主题图标不再携带状态徽标，徽标仅由剧本资源侧栏渲染；自定义主角与危险场景改为文字明确的双态选择。
- 2026-09-19：角色资源目录保持身份与版本组织职责；剧本“角色阵容”卡片的副字段改为作者与素材说明，并增加复用运行时角色面板的只读阵容预览。
- 2026-09-19：修复双态选择的激活样式被工作区通用按钮样式覆盖的问题；选中项同时使用勾号、“当前”文字、高对比底色与
  `aria-pressed` 表达状态。
- 2026-09-19：重做“组建角色阵容”弹窗的响应式宽度链：桌面双栏取消硬编码最小列宽，中等与移动宽度改为身份列表、版本详情各自滚动的上下分区；角色卡、版本卡和底栏不再受宿主按钮高度或横排空间挤压。
- 2026-09-19：确认阵容卡持续挤压的根因是身份卡和版本卡仍使用原生按钮、被真实宿主按钮规则覆盖；两类卡片改为带键盘语义的交互容器，从结构上隔离宿主按钮样式。剧本标题图标改为复用共享主题图标组件，直接使用各 SVG 主题定义的真实颜色与辉光。
