# Grill Me Discussion Draft

## 当前目标

- 恢复“尘史使徒创意工坊”相关工作的真实历史，区分已完成资产、历史实验和尚未落地的产品设想。
- 确认用户口中的“创意工坊”是否对应代码中长期使用的名称“人物创建”，并恢复该功能的分支历史与完成度。

## 已确认结论

- 所有 58 个本地及远端跟踪引用中都不存在“创意工坊”这一文本名称，Git 历史的精确字符串检索也没有结果。
- 尘史使徒内部存在与“创意工坊”高度吻合的实际模块，代码名为“人物创建”：提交 `d5abda16` 于 2026-02-09 首次引入，之后经历 UI 优化、移动端适配、无限点数、术能力、交互、叙事节奏、写作指导、MVU 迁移、关系矩阵、组件解耦、配置导入导出和初始物资平衡。
- 当前 `feature-secret-mvu` 保存了该模块的最新尘史版本：四步流程“倒影 / 登阶 / 羁绊 / 铭刻”，并由 `开场设置.vue` 在“被遗忘者”剧本启动后进入。
- 当前功能已经能选择基础身份、出生地和外貌，生成性格侧写，分配术与派生属性，编辑主要角色羁绊，选择叙事节奏，将剩余点数兑换为金钱/异质，导入导出 JSON 配置，提交 MVU 数据、注入开局正文并触发 `生成user人设`。
- `feature-secret` 保存的是解耦前的 ERA 时代版本；当前分支随后完成了 MVU 迁移和四组件拆分。
- `O:\St Working\dust-laden-obdurant` 保存的是较早的单文件快照；它不是额外分支，也不是最新实现。
- `feature-moelotl` 和 `feature-goblin` 虽然包含人物创建的祖先提交，但各自的独有工作分别集中在成就/画廊和哥杀模块，没有新增尘史创意工坊能力。
- `myIdea` 保存了 2026-01-19 制作的 27 张教程截图，覆盖：拉取模板、理解项目结构、创建新前端、由 ERA JSON 编写 `StatData.d.ts`、用 AI 设计 Vue 模块、配置路由、构建排错、导入 SillyTavern、变量字段排错和本地实时预览。
- Git 的 `template` 分支保存了完整的 `myIdea/写前端教程.md`，其明确目标是教 ERA 用户借助 Tavern Helper 模板和 AI 制作角色卡状态栏；文末只承诺未来补充“写卡和跑图经验”。
- 同一分支的提交 `83fcf212` 保存了一套 `src/教程角色卡` 示例实现，包括入口、路由、Store、类型、世界/人物/设置页面及示例数据。
- `O:\St Working\dust-laden-obdurant` 没有 Git 元数据，且其中 27 张截图与当前工作区逐文件哈希一致；目前没有发现额外的创意工坊成果。
- `template` 分支的教程与示例角色卡是另一条独立历史，不是人物创建模块的主体。

## 关键依据与反例

- 依据：人物创建的提交历史连续、入口完整，并直接修改尘史的世界、用户角色、主要角色关系和系统叙事配置。
- 依据：页面当前标题是“重塑自我”，路由仍叫“人物创建”；“创意工坊”更可能是讨论时的产品名，从未同步进代码命名或 Spec。
- 风险：当前实现虽然功能广，但缺少对应长期 Spec、契约测试和宿主验收记录，不能仅凭页面存在断言流程已经稳定完成。

## 待决策问题

- 用户确认“创意工坊”是否就是这套“重塑自我 / 人物创建”流程；若不是，需要提供记忆中的一个可区分特征继续定位。

## 实施约束

- 不把 `template` 分支的 ERA 教程代码与当前 MVU 人物创建流程混为一体。
- 在继续修改人物创建前，先补齐其业务规则、数据写入契约和可复现验收路径。
