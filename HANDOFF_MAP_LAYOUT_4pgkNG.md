# Handoff：动态节点地图的空间化 SVG 与相对布局算法

## 当前目标

继续优化“尘史使徒”的动态嵌套地图表现力，使地图在不放弃“节点 + 子地图”数据结构的前提下，能够通过 **SVG 的人工设计尺寸、长宽比与形状** 表达地点的空间性质，并由前端布局算法保证大图标不会因为距离过近或缩放而互相挤压。

核心方向不是做静态 GIS/底图，也不是让 AI 生成复杂多边形，而是把现有节点地图升级成一种 **递归、多尺度、碰撞感知的符号地图**。

当前最重要的原则修正：

> **图标最终大小由 SVG 本身的人为 `width` / `height` 决定。**
> 可以参考节点在地图 `方位` 中的真实范围与比例来设计 SVG，但前端不要自动根据 `方位` 反推图标大小。

也就是说：
- `方位` 决定节点之间的相对位置、方向和大致距离；
- SVG 的 `width` / `height` 决定这个地点在地图上的视觉占地；
- 前端算法负责在保持相对空间关系的前提下，为这些视觉占地提供最低安全距离。

---

## 已完成的工作

### 1. 已验证“空间化 SVG”方向可行

此前顶层地图所有节点基本都是统一大小的 POI icon，表现力很弱。已经进行了多轮试验，把顶层节点改成不同尺寸、不同长宽比的 SVG，例如：

- 龙牙山脉：纵向大型符号，表达山脉屏障；
- 迷雾之海：横向大型符号，表达海域；
- 大荒漠 / 焚风之地 / 腐烂泥沼：横向区域纹样；
- 棱镜森林：较大群落式符号；
- 王国、聚集地：相对点状；
- 永恒冻土：大范围冰原符号。

实际导入后，展开状态下视觉表现明显提升，证明“通过 SVG 形状 + 尺寸表达空间关系”的方向成立。

用户明确表示目前这套 **单色金线、`fill="none"`、几何地图线稿** 风格满意，下层图标后续也大概率会逐步替换成同一套视觉语言。

### 2. 已解除 SVG 固定尺寸限制

当前渲染链路已经支持读取 SVG 根节点的 `width` / `height` / `viewBox`，并将实际尺寸传给地图节点。

相关文件：

- `map.ts`
- `MapExplorer.vue`
- `MapSvgIcon.vue`

当前 `MapSvgIcon.vue` 会让 SVG 充满外层 `.node`：

```css
.map-svg-icon,
.map-svg-icon :deep(svg) {
  width: 100% !important;
  height: 100% !important;
}
```

`MapExplorer.vue` 中则把解析出的 SVG 宽高写入：

```ts
--map-svg-width
--map-svg-height
```

### 3. 已定位当前缩放问题

当前节点位置使用：

```ts
scale = baseScale * transform.k
```

所以节点中心距离会随缩放变化。

但显式 SVG `width` / `height` 是固定 px，并不会随着 `transform.k` 同步缩放。

因此地图缩小时会出现：

- 节点中心越来越近；
- 大 SVG 仍保持原像素大小；
- 大地貌互相堆叠；
- 标签也挤成一团。

这就是当前缩放视图下碰撞严重的直接原因。

### 4. 已发现 `fit()` 目前只考虑节点中心

当前 `fit()` 只根据所有节点的 `displayX/displayY` 最大最小值决定 `baseScale`，不考虑 SVG 实际宽高，所以大型 SVG 的视觉包围盒并没有进入初始视图拟合。

### 5. 已发现 `viewBox` 当前被错误地当成默认显示尺寸来源

`map.ts` 当前存在：

```ts
const mapViewBoxPixelScale = 2;
```

如果 SVG 没写 `width/height`，会用：

```ts
width = viewBoxWidth * 2
height = viewBoxHeight * 2
```

这会让 `viewBox` 同时承担“内部坐标系”和“默认显示尺寸”两种职责。

后续建议拆开：

- `viewBox` 只负责 SVG 内部比例；
- 显式 `width/height` 才代表作者希望的地图视觉占地；
- SVG 未声明 `width/height` 时再回退到地图默认 iconSize。

### 6. 已发现坐标轴语义与渲染存在不一致

地图提示词定义：

- x 向右
- y 向下

但当前 `MapExplorer.vue` 实际渲染是：

```ts
left = displayY * scale
top  = -displayX * scale
```

即：

- 数据 y -> 屏幕水平；
- 数据 x -> 屏幕垂直且正方向向上。

现有地图数据可能已经在这个历史行为上形成适配，因此不要在本轮布局算法中顺手修正，除非单独做迁移方案。

---

## 关键决策

### 1. 图标大小不自动由 `方位` 计算

这是最新、必须遵守的决策。

不要实现：

```ts
iconWidth = f(xmax - xmin)
iconHeight = f(ymax - ymin)
```

也不要让 renderer 自动把真实坐标范围映射成 SVG 尺寸。

正确关系是：

```text
方位范围
  ↓
供人 / AI 设计 SVG 时参考真实空间比例
  ↓
作者自行决定 SVG width / height
  ↓
renderer 尊重 SVG 的人为尺寸
```

这样可以保留设计自由度，因为真实地理尺度和“地图上应有的视觉表现力”并不是机械的一一对应关系。

### 2. `方位` 表达相对空间，不应理解成绝对像素距离

地图的重点是：

- 谁在谁的什么方向；
- 谁离谁更近；
- 大致距离比例；
- 节点之间的拓扑关系。

不是要求 `(x,y)` 绝对线性映射到屏幕像素后必须一毫米不差。

因此 renderer 可以为了可读性“撑开”过近节点，但不能随意改变方向关系和总体距离排序。

### 3. 使用“碰撞箱 + 相对空间布局”，而不是简单避让

每个当前子地图的节点应有：

```text
semanticPosition = 原始方位中点
visualPosition   = renderer 计算出的最终显示位置
```

`visualPosition` 只用于显示，不写回 JSON。

每个节点有自己的视觉碰撞箱，至少包含：

- SVG 实际 width / height；
- 必要安全边距；
- 后续可考虑 label 占位。

当两个节点的原始相对距离不足以容纳两个碰撞箱时，布局算法把它们沿原始方向撑开。

核心思想可以写成：

```text
视觉距离 = max(原始比例距离, 防碰撞所需最小距离)
```

重点不是精确使用这个公式，而是遵守其语义。

### 4. 尽量保持原始方向和距离比例

发生碰撞时：

- 西边仍然在西边；
- 北边仍然在北边；
- 原本明显更远的节点仍应看起来更远；
- 优先沿两个节点的原始连线方向拉开；
- 不要使用普通 force layout 把节点随意弹到其它方向。

### 5. 同位置节点也必须可读

典型案例：

- `白金王宫` x/y = 0/0
- `狮子之丘` x/y = 0/0

当前二维算法会完全重叠。

由于它们 z 不同，而且语义是“王宫位于狮子之丘之上”，后续布局可以适度利用 z 产生弱视觉偏移，例如将高 z 节点在屏幕上稍微向上偏移。

如果 x/y/z 全部完全相同，则使用**稳定、确定性的局部分散规则**，不能随机，否则每次打开地图布局会变化。

### 6. 子地图机制是本方案能够成立的重要前提

当前界面只渲染当前节点的直接 `子地图`，不会把整个递归地图一次性全部展开。

因此：

- 世界层只需要解决十几个世界级节点；
- 国家层只解决几个城市；
- 城市层只解决几个城区 / 设施；
- 更深层再处理建筑与房间。

这天然形成多尺度地图，能够避免“大山脉和咖啡店同时争夺同一个画布”的问题。

### 7. 缩放行为：不能出现“地图越缩小，图标相对越大”

这点是下一阶段算法最关键的体验要求。

当前错误行为：

```text
节点距离随 zoom 缩小
SVG 固定 px
=> 图标在地图里相对越来越大
```

目标行为：

```text
正常缩放阶段：
空间距离和 SVG 同步按比例缩放

缩小到即将低于可读 / 碰撞阈值：
不再继续破坏可读性，碰撞布局开始维持最低视觉间距
```

反过来，放大地图时：

> **先逐渐缩小为了防碰撞而额外增加的“相对视觉距离”，回归真实相对比例；不能出现放大地图以后图标在布局中反而显得更夸张。**

换句话说：

```text
小比例尺：碰撞约束强，节点可能被适当撑开
↓ 放大
碰撞带来的额外距离逐渐释放
↓
大比例尺：越来越接近原始方位比例
```

SVG 本身也应随着地图缩放，而不是永久固定 px。

### 8. SVG 可以更加大胆

如果上述布局算法成立，则以后不需要担心大型 SVG 必然撞车。

可以更大胆地让 SVG 接近地点在该层地图中的空间规模，例如：

- 山脉可以真正成为长条屏障；
- 海域可以横向占据很大面积；
- 森林 / 沼泽 / 荒漠可以明显是“区域”；
- 街区 / 下水道 / 隘口等中层节点也可以使用中型空间符号；
- 建筑 / 店铺 / 房间仍保持紧凑点状图标。

但大小仍然由 SVG 作者人工决定，只“参考”真实地图比例，不机械映射。

---

## 尚未完成的工作

### 优先级 P0：重构当前地图布局算法

下一位 agent 应优先设计 / 实现：

1. **SVG 随 zoom 正常缩放**
   - 不再让显式 width/height 永久固定屏幕 px；
   - 保留合理最低可读尺寸，但达到最低尺寸后不能继续把节点距离无限压缩。

2. **碰撞感知的相对空间布局**
   - 输入：semantic position + SVG 人工尺寸；
   - 输出：visual position；
   - 不修改地图 JSON；
   - 保持方向和距离比例；
   - 防止大型 SVG 初始即重叠。

3. **放大时逐渐释放碰撞偏移**
   - zoom 越大，visual position 越接近 semantic position；
   - zoom 越小，碰撞约束越强；
   - 不允许“放大以后为了避碰反而越来越分散”。

4. **升级 `fit()`**
   - 不能只看节点中心；
   - 应考虑布局完成后的节点视觉包围盒。

5. **修正 `mapSvgDisplaySize()` 的默认尺寸语义**
   - `viewBox` 不应再自动乘 `mapViewBoxPixelScale` 变成实际显示尺寸；
   - 未声明 width/height 的旧 SVG 应回退到默认 iconSize，保持兼容。

### P1：标签策略

标签目前固定放在 SVG 下方，并始终显示。

后续应研究：

- 标签与 SVG 的碰撞是否一起进入包围盒；
- 远景时是否需要标签 LOD / 碰撞淘汰；
- 当前地点 / 聚焦地点标签应始终显示；
- 不建议为了标签碰撞大幅移动大型地貌本体。

### P1：下层图标整体重绘

用户满意当前顶层的新视觉风格，后续预计会把下层旧图标逐步替换为同风格。

建议优先区分：

- 普通点状地点：王宫、礼拜堂、咖啡店、港口、市场、矿洞、墓地、学院、旅店；
- 中型空间节点：贵族街、平民街、炉火区、崖灯区、下水道迷宫、活木根须地、刃风隘口、龙语回廊等。

不要先批量重画全部图标；应先把布局算法稳定，否则很难评价“大图标”的真实效果。

---

## 当前风险 / 阻塞

### 1. 两个大型 SVG 初始坐标过近时会直接重叠

现有算法没有碰撞约束，这是当前最直接的问题。

### 2. 缩放后 SVG 相对地图越来越大

节点中心距离乘 zoom，而 SVG 固定 px，是目前缩小视图严重拥挤的根本原因。

### 3. `viewBox` 与显示尺寸耦合

如果不拆开，后续 AI 只改变 SVG 内部坐标系也可能意外改变地图显示尺寸。

### 4. 现有坐标轴实现与提示词语义不一致

本轮不要顺手修，避免把布局重构和地图坐标迁移混成一个任务。

### 5. 不要把“防碰撞”实现成普通 force layout

普通力导向布局容易让节点改变方位关系，这会破坏地图的核心语义。

---

## 重要文件

当前相关上传文件：

- `MapExplorer.vue`：地图节点位置、缩放、fit、节点样式的核心实现；本轮最主要修改点。
- `map.ts`：SVG sanitize / render / `mapSvgDisplaySize()`；需要调整 viewBox 与默认显示尺寸关系。
- `MapSvgIcon.vue`：SVG 实际渲染组件，目前 SVG 填满 node 容器。
- `MapLocationPicker.vue`
- `MapAssetEditor.vue`
- `MapFullscreenDialog.vue`
- `尘史地图提示词-泰拉大陆完整嵌套地图。.txt`：当前地图数据、生成规则与完整 MapEntry。

已有实验产物（无需重复生成完整内容，除非继续导入测试）：

- `泰拉大陆-顶层空间化图标-大胆版-全量.json`
- `泰拉大陆-顶层空间化图标-激进版-全量.json`

---

## 推荐下一步

不要继续先画图标。

下一位 agent 应先基于现有 `MapExplorer.vue`，提出并实现一个尽量简单、确定性强的 **碰撞感知相对布局算法**，并用以下三类案例验证：

1. 两个正常距离的小节点；
2. 两个坐标很近的大 SVG；
3. `白金王宫` 与 `狮子之丘` 这种 x/y 完全相同、z 不同的节点。

同时验证缩放行为：

- 缩小时图标不会相对地图越来越大；
- 小比例尺下有最低碰撞间距；
- 放大后碰撞导致的额外间距会逐渐释放；
- 最终趋向原始相对空间关系。

实现时优先保持算法和数据协议简单，不给 AI 增加复杂必填字段。

---

## 下一位 agent 应优先使用的 skill

如工作区存在测试 / 前端实现相关 Skill，优先读取与当前 Vue / TypeScript 修改和测试有关的 Skill。

交接文档本身遵循本轮提供的 `handoff` Skill：只记录必要上下文与决策，不重复完整 PRD、完整 JSON 或源文件内容。
